/**
 * 共有ビューのルートコンポーネント。
 * P1-B で実装される（スキャン/送信/受信表示の3タブ、軽量維持: core+Preact のみ import 可）。
 */
export function ShareApp() {
  return (
    <main style={{ padding: '2rem', textAlign: 'center' }}>
      <h1>棚番長 — 共有</h1>
      <p>構築中（P0 スキャフォールド）</p>
    </main>
  );
}
