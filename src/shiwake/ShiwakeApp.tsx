/**
 * 仕分番長のルートコンポーネント。
 * P1-E で実装される（明細OCR/要注意/メモの3タブ + 辞書還流 + 客注照合）。
 */
export function ShiwakeApp() {
  return (
    <main style={{ padding: '2rem', textAlign: 'center' }}>
      <h1>仕分番長 v2</h1>
      <p>構築中（P0 スキャフォールド）</p>
    </main>
  );
}
